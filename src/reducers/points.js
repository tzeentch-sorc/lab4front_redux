import {GET_POINTS} from "../actions/setPoints";

export default function points(state = [], action = {}) {
    switch (action.type) {
        // case GET_POINTS:
        //     return {
        //       points: state
        //     };
        default: return state;
    }
}